name: MangasmiOS
options:
  bundleIdPrefix: com.mangasm
  deploymentTarget:
    iOS: "17.0"
packages:
  MangasmApp:
    path: .
configFiles:
  Debug: App/iOS/Secrets.xcconfig
  Release: App/iOS/Secrets.xcconfig
targets:
  Mangasm:
    type: application
    platform: iOS
    sources:
      - App/iOS
    dependencies:
      - package: MangasmApp
        product: MangasmApp
    entitlements:
      path: App/iOS/Mangasm.entitlements
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        PRODUCT_BUNDLE_IDENTIFIER: com.mangasm.app
        DEVELOPMENT_TEAM: 854XZ2543V
        CODE_SIGN_STYLE: Automatic
        CODE_SIGN_ENTITLEMENTS: App/iOS/Mangasm.entitlements
        PRODUCT_NAME: Mangasm
        MARKETING_VERSION: "1.0.1"
        CURRENT_PROJECT_VERSION: "5"
        TARGETED_DEVICE_FAMILY: "1"
        INFOPLIST_KEY_UIApplicationSupportsIndirectInputEvents: YES
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_ITSAppUsesNonExemptEncryption: NO
        INFOPLIST_KEY_SUPABASE_URL: $(SUPABASE_URL)
        INFOPLIST_KEY_SUPABASE_PUBLISHABLE_KEY: $(SUPABASE_PUBLISHABLE_KEY)
        SWIFT_VERSION: "6.0"
  MangasmTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - Tests/iOS/Unit
      - path: Mangasm.storekit
        buildPhase: resources
    dependencies:
      - target: Mangasm
      - package: MangasmApp
        product: MangasmApp
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        SWIFT_VERSION: "6.0"
  MangasmUITests:
    type: bundle.ui-testing
    platform: iOS
    sources:
      - Tests/iOS/UI
    dependencies:
      - target: Mangasm
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        SWIFT_VERSION: "6.0"
schemes:
  Mangasm:
    build:
      targets:
        Mangasm: all
        MangasmTests: [test]
        MangasmUITests: [test]
    run:
      config: Debug
      storeKitConfiguration: Mangasm.storekit
    test:
      config: Debug
      # StoreKitFlowTests drives StoreKit via SKTestSession (programmatic), so no
      # scheme-level storeKitConfiguration here (the two conflict).
      targets:
        - MangasmTests
        - MangasmUITests
    profile:
      config: Release
    analyze:
      config: Debug
    archive:
      config: Release
