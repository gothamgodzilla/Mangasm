{
  "name": "mangasm",
  "private": true,
  "description": "Mangasm monorepo — pre-commit tooling (Swift app + static web)",
  "scripts": {
    "prepare": "husky",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "echo 'No TS typecheck at repo root (Swift package)'",
    "test": "swift test --parallel"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.1.1",
    "prettier": "^3.9.6"
  }
}
